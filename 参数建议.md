# DriveGPT 训练参数建议

本文档详细说明 DriveGPT 训练过程中的各项参数及其调整建议。

---

## 一、路径配置

| 参数名 | 当前值 | 说明 |
|--------|--------|------|
| output_dir | E:\DriveGPT\output | 训练输出目录 |
| model_name | E:\DriveGPT\model\Qwen3-VL-2B-Instruct | 基础模型路径 |
| image_root | E:\DriveGPT\image | 训练图片根目录 |
| json_path | E:\DriveGPT\data\BDD_X_training_label_zh.json | 训练数据JSON路径 |

---

## 二、训练参数

### 2.1 num_epochs（训练轮数）
- **当前值**: 1
- **可选**: 1, 2, 3, 5
- **建议**:
  - 初次训练：1-2轮，快速验证代码正确性
  - 正式训练：2-3轮，平衡效果和过拟合风险
  - 数据充足：3-5轮，充分学习数据特征

### 2.2 batch_size（批次大小）
- **当前值**: 2
- **可选**: 1, 2, 4, 8
- **建议**:
  - 8GB显存：batch_size=1
  - 12GB显存：batch_size=2
  - 16GB+显存：batch_size=4或8

### 2.3 gradient_accumulation_steps（梯度累积）
- **当前值**: 4
- **可选**: 1, 2, 4, 8
- **作用**: 模拟大batch训练，实际batch = batch_size × 累积步数
- **建议**: batch_size=1时设为8，batch_size=2时设为4

### 2.4 learning_rate（学习率）
- **当前值**: 5e-5
- **可选**: 1e-5, 2e-5, 5e-5, 1e-4
- **建议**:
  - LoRA微调：5e-5
  - 训练不稳定：降至2e-5
  - 全参数训练：1e-5或2e-5

### 2.5 warmup_ratio（预热比例）
- **当前值**: 0.1
- **可选**: 0.03, 0.05, 0.1, 0.2
- **作用**: 前10%步数学习率从0线性增加

### 2.6 max_length（最大序列长度）
- **当前值**: 1024
- **可选**: 512, 1024, 2048
- **建议**: BDD-X数据较短，512或1024即可

---

## 三、LoRA参数

### 3.1 lora_r（LoRA秩）
- **当前值**: 128
- **可选**: 8, 16, 32, 64, 128, 256
- **建议**:
  - 简单任务：16或32
  - 一般任务：64或128（推荐）
  - 复杂任务：256

### 3.2 lora_alpha（LoRA缩放）
- **当前值**: 256
- **建议**: 设为 lora_r 的2倍

### 3.3 lora_dropout
- **当前值**: 0.05
- **可选**: 0.0, 0.05, 0.1, 0.2
- **作用**: 防止过拟合

---

## 四、Trainer参数

### 4.1 weight_decay（权重衰减）
- **当前值**: 0.01
- **作用**: L2正则化，防止过拟合

### 4.2 lr_scheduler_type
- **当前值**: cosine
- **可选**: linear, cosine, constant

### 4.3 fp16 / bf16
- **当前值**: bf16=True
- **建议**: RTX 30/40系显卡使用bf16=True

### 4.4 gradient_checkpointing
- **当前值**: False
- **作用**: 显存不足时设为True，省30%显存但训练变慢

---

## 五、推荐配置

### 8GB显存（如RTX 3070）
```python
batch_size = 1
gradient_accumulation_steps = 8
lora_r = 64
lora_alpha = 128
gradient_checkpointing = True
```

### 12GB显存（如RTX 3060）
```python
batch_size = 2
gradient_accumulation_steps = 4
lora_r = 128
lora_alpha = 256
gradient_checkpointing = False
```

### 16GB+显存（如RTX 4090）
```python
batch_size = 4
gradient_accumulation_steps = 2
lora_r = 256
lora_alpha = 512
gradient_checkpointing = False
```

---

## 六、常见问题

### CUDA Out of Memory
1. batch_size减至1
2. 增大gradient_accumulation_steps
3. 启用gradient_checkpointing = True
4. 减小lora_r至64或32

### Loss不下降或震荡
1. 降低learning_rate至2e-5
2. 增大warmup_ratio至0.2
3. 检查数据路径是否正确

### 训练速度过慢
1. 增大batch_size（显存允许）
2. 减小gradient_accumulation_steps
3. 关闭gradient_checkpointing
4. 增大dataloader_num_workers
